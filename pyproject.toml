[project]
name = "movie-analytics"
version = "0.1.0"
description = "Movie ratings + box office financial analytics data website (course project)"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pandas>=3.0.6",
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
]
